from setuptools import setup, find_packages

setup(
    name='robustlmm',
    version='0.1.0',
    description='A package for robust lmm experiment',
    author='Anonymous',
    author_email='Anonymous',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies your package needs here
    ],
)
